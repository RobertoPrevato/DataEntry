* use Tether library to position tooltips of the DomMarker
* `required` rule should produce different messages for select elements or checkboxes (like: Please select a value)

Document options:
disableAutoFocus